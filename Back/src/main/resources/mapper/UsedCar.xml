<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.rental.mapper.UsedCarMapper">

    <!-- 중고차 목록 조회 -->
<select id="getAllUsedCars" resultType="com.rental.dto.UsedCarDTO">
    SELECT vehicle_name AS vehicleName,
           vehicle_no AS vehicleNo,
           dealer_no AS dealerNo,
           vehicle_type AS vehicleType,
           brand AS brand,
           model_year AS modelYear,
           price AS price,
           color AS color,
           dealer_location AS dealerLocation,
           fuel_type AS fuelType,
           transmission AS transmission,
           drive_type AS driveType,
           used_car_image AS usedCarImage,
           vehicle_plate AS vehiclePlate,
           car_km AS carKm
    FROM used_car
</select>
</mapper>