<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.rental.mapper.UsedCarImageMapper">

    <!-- 특정 차량의 이미지 목록 조회 -->
    <select id="getImagesByVehicleNo" resultType="com.rental.dto.UsedCarImageDTO">
        SELECT VEHICLE_NO, IMAGE_URL, MAIN_IMAGE
        FROM USED_CAR_IMAGE 
        WHERE VEHICLE_NO = #{vehicleNo}
    </select>

    <!-- 새로운 이미지 추가 -->
   <insert id="insertUsedCarImage">
        INSERT INTO USED_CAR_IMAGE (IMAGE_ID, VEHICLE_NO, IMAGE_URL)
        VALUES (USED_CAR_IMAGE_SEQ.NEXTVAL, #{vehicleNo}, #{imageUrl})
    </insert>

    <!-- 특정 이미지 URL을 기준으로 삭제 -->
    <delete id="deleteImageByUrl">
        DELETE FROM USED_CAR_IMAGE WHERE IMAGE_URL = #{imageUrl}
    </delete>

    <!--  기존 대표 이미지를 'N'으로 업데이트 -->
    <update id="updateMainImageStatusByVehicleNo">
        UPDATE USED_CAR_IMAGE
        SET MAIN_IMAGE = 'N'
        WHERE VEHICLE_NO = #{vehicleNo} AND MAIN_IMAGE = 'Y'
    </update>

    <!--  새로운 대표 이미지를 'Y'로 설정 -->
    <update id="setNewMainImage">
        UPDATE USED_CAR_IMAGE
        SET MAIN_IMAGE = 'Y'
        WHERE VEHICLE_NO = #{vehicleNo} AND IMAGE_URL = #{imageUrl}
    </update>
    <!--중고차 삭제하면서 이미지 삭제-->
   <delete id="deleteImagesByVehicleNo" parameterType="string">
    DELETE FROM USED_CAR_IMAGE WHERE VEHICLE_NO = #{vehicleNo}
    </delete>

</mapper>
